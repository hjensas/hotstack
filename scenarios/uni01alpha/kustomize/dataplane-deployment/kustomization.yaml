---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Reference the cloned architecture repo EDPM deployment component.
# The ../../ path resolves from manifests_dir (/home/zuul/manifests/kustomize/dataplane-deployment)
# back to /home/zuul/manifests/architecture/dt/uni01alpha/edpm
# Note: Data plane components are in dt/ (not examples/dt/) in the architecture repo
components:
  - ../../architecture/dt/uni01alpha/edpm
